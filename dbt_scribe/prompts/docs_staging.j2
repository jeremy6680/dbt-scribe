Generate dbt documentation for the staging model `{{ model.name }}`.

Return a JSON object with exactly these top-level keys:
- `model_description`: concise one-sentence model description
- `docs_block_content`: longer markdown docs block content
- `columns`: object mapping column names to descriptions

Model context:
- unique_id: {{ model.unique_id }}
- fqn: {{ model.fqn | join(".") }}
- layer: {{ model.layer.value }}
- materialized: {{ model.materialized }}
- upstream nodes: {{ model.depends_on_nodes | join(", ") }}

Existing model description:
{{ model.description or "" }}

Columns:
{% for column in model.columns.values() %}
- {{ column.name }} ({{ column.data_type or "unknown" }}, {{ column.column_type.value }})
  existing description: {{ column.description or "" }}
  needs_doc: {{ column.needs_doc }}
  sql_expression: {{ column.sql_expression or "" }}
{% endfor %}

Compiled SQL:
```sql
{{ model.compiled_code }}
```

Shared columns configured for this project:
{{ config.docs.shared_columns | join(", ") }}
